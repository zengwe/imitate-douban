import { TimeHorizonPipe } from './time-horizon.pipe';

describe('TimeHorizonPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeHorizonPipe();
    expect(pipe).toBeTruthy();
  });
});
